// Parametros del objeto
depth = Profundidades.Backstage;
image_speed = 0.3;

// Variables
valor_aumento_rings = 1;
efecto_visual = obj_efecto_destello_ring;
efecto_sonido = choose(snd_ring_izquierdo, snd_ring_derecho);